# Architectural Template

A reusable TypeScript/Next.js repository template built around strict role purity, one-way dependencies, thin framework surfaces, explicit orchestration, and conservative sharing.

This repository intentionally contains a small wired `examples` feature so the architecture is executable rather than a forest of empty folders.

The detailed architecture guide and runnable scaffold are added in the next commit.
